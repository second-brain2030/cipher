# List of MCP servers to use
mcpServers: {}

# Local Qwen3 Configuration using OpenAI-compatible endpoint
llm:
  provider: openai
  model: Qwen3-32B-AWQ
  apiKey: EMPTY
  baseURL: http://10.103.1.2:9001/v1
  maxIterations: 50

# Embedding configuration - disabling for now as user only specified LLM
embedding:
  disabled: true

systemPrompt:
  enabled: true
  content: |
    You are an AI programming assistant focused on coding and reasoning tasks.