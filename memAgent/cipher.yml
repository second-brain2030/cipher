# List of MCP servers to use
mcpServers: {}

# Local Qwen3 Configuration using OpenAI-compatible endpoint
llm:
  provider: openai
  model: Qwen3-32B-AWQ
  apiKey: EMPTY
  baseURL: http://10.103.1.2:9001/v1
  maxIterations: 50

# Embedding configuration - using local Ollama with BGE-M3 (excellent for Chinese+English)
embedding:
  type: ollama
  model: bge-m3
  baseUrl: http://localhost:11434

systemPrompt:
  enabled: true
  content: |
    You are an AI programming assistant focused on coding and reasoning tasks.